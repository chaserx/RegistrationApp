<%= simple_form_for(@emailannouncement, :html => {:class => "form-vertical"}) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :announcement_body %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Send Now" %>
  </div>
<% end %>
